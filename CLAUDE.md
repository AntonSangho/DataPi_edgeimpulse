# CLAUDE.md — 이 저장소에서 일하는 방식

DataPi v0.3(Pico W)에 Edge Impulse를 적용하는 **방법을 찾는** 프로젝트다.
제품이나 데모가 아니라 `docs/DATAPI_EI_GUIDE.md`(Phase 4)가 산출물이다.
따라서 **정확도를 올리는 것보다 왜 그렇게 됐는지가 기록되는 것**이 중요하다.

---

## 고정값 — 바꾸려면 데이터셋도 다시 만들어야 한다

| 항목 | 값 |
|---|---|
| BH1750 | H-Resolution, MTreg **31**, I2C0 (GP4=SDA / GP5=SCL), 0x23 |
| 샘플 주기 | **62500 us = 정확히 16 Hz** (`SAMPLE_PERIOD_US`) |
| 창 / stride | 2000 ms (32 샘플) / 500 ms |
| EI 프로젝트 | `datapi light sensor` — ID **1079757** |
| 축 이름 | **`lux`** (Phase 3의 C fusion 디스크립터와 일치해야 함) |

- `--frequency N`과 `SAMPLE_PERIOD_US`는 **항상 같이** 바꾼다.
  어긋나면 주파수로 구분되는 클래스의 스펙트럼이 밀린다.
- L-Resolution 모드는 쓰지 않는다 (미완결 값 반환).
- **test 세트(90 lux 세션)는 다시 찍지 않는다.** 고정 비교 기준이다.

---

## 작업 규칙

### 1. 추정과 실측을 구분해서 말한다

이 프로젝트에서 `wave`/`swipe` 분리 가능성을 네 번 추정했고 답이 계속 뒤집혔다.
매번 짧은 샘플이나 저차원 투영으로 판단한 탓이다.

- 30초 미만 데이터, 3D 투영, 눈대중 카운트에서 나온 것은 **가설**이다. 그렇게 부른다.
- 숫자를 단정적으로 쓰지 않는다. "3.5~4배 차이난다"가 아니라
  "10초 샘플에서 눈으로 세면 3.5~4배로 보인다".
- 가설을 근거로 코드나 파라미터를 바꾸지 않는다. 실측을 먼저 한다.

### 2. 백분율은 분모를 확인하기 전까지 해석하지 않는다

1차 학습에서 `cover`의 "11.1% 혼동"을 두고 인과 이론을 만들었는데,
**9개 창 중 1개**였다. 기약분수로 되돌려 보면 표본 크기가 나온다.

```python
from fractions import Fraction
Fraction(0.111).limit_denominator(60)   # 1/9
```

### 3. 도구가 무엇을 하는지는 문서를 읽고 말한다

Spectral Analysis 블록이 RMS를 출력한다는 사실을 확인하지 않고
"로그 스펙트럼이 절대 레벨을 지운다"는 설명을 만들어 문서에 박았다.
EI 블록·CLI 동작을 설명할 때는 `WebFetch`로 공식 문서를 먼저 확인한다.

### 4. 하드웨어 검증은 사용자가 한다 — 포트 점유를 확인한다

`/dev/ttyACM0`은 하나의 프로세스만 연다. `edge-impulse-data-forwarder`가
붙어 있으면 `mpremote`가 실패한다.

```bash
fuser -v /dev/ttyACM0     # 누가 잡고 있는지
```

사용자의 포워더를 임의로 죽이지 않는다. 검증이 필요하면 **명시적으로 요청**하고,
"껐습니다"를 확인한 뒤 진행한다.

제스처 수행이 필요한 측정은 반드시 사용자에게 요청한다. 손동작 없이 돌릴 수 있는
부분(`characterize.py`)과 필요한 부분(`gesture_range.py`)은 스크립트를 나눠 둔다.

### 5. GUI 도구는 "입력할 값"이 아니라 "확인할 출력"으로 안내한다

Studio 설정을 알려주는 것만으로는 부족하다. 라벨 끝 공백, Sample length,
축 이름, 샘플 수 — 넷 다 조용히 실패했고 **세 번 재녹화**했다.

수집을 안내할 때는 포워더 로그에서 확인할 줄을 함께 준다:

```
label: 'idle'                              ← 끝 공백 없음
length: 60000                              ← 10000 아님
sensor: 'Sensor with 1 axes (lux)'         ← 라벨 목록 아님
Reading data from device OK (960 samples)  ← 60초=960, 30초=480
```

### 6. 실패와 판단 번복을 지운 자리에 덮어쓰지 않는다

`docs/`가 원본, GitHub Issue 코멘트가 시간순 기록이다.
틀린 판단은 **삭제하지 않고 정정 항목을 덧붙인다.** 어떻게 틀렸는지가
이 프로젝트의 산출물에 들어간다.

커밋 메시지에는 "무엇을"이 아니라 **"왜 그렇게 바꿨는지"**를 쓴다.

---

## 문서 규칙

- **한국어로 쓴다.** 코드 식별자와 출력 문자열은 영어.
- 표를 적극적으로 쓴다 (`Datapi_Bringup` README 스타일).
- 측정값에는 조건을 함께 적는다 (조명, 배경 lux, 세션 길이).
- 파일을 추가하면 README의 파일 표에 한 줄 넣는다.
- 새 실측이 기존 서술을 뒤집으면 **양쪽을 다 남긴다.**

## 코드 규칙

- MicroPython 코드는 `src/`, 공용 루틴은 `src/lib/`.
- `ei_forwarder.py`는 **stdout이 곧 데이터 스트림**이다.
  어떤 진단 출력도 섞으면 안 된다 (`make_sensor(quiet=True)`).
- 하드웨어 루프는 예외로 죽지 않게 한다. 스트림이 조용히 끊기면
  60초 녹화가 25초로 잘린 채 "OK"로 업로드된다.
- 드라이버 수정은 원본과의 차이를 파일 상단 주석에 남긴다
  (`src/lib/bh1750.py`의 버그 2건 참고).

## 관련 저장소

| 경로 | 용도 |
|---|---|
| `~/projects/Datapi_Bringup` | DataPi v0.3 핀맵·드라이버 원본 |
| `~/projects/firmware-pi-rp2xxx` | EI 공식 RP2040 펌웨어 (Phase 3에서 포크) |
| `~/projects/pdf-tutorial/output/AI at the Edge - full book - compressed.md` | 참고서 |
